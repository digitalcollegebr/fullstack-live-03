// O operador ternário em JavaScript é uma forma concisa de escrever uma expressão condicional.
// é usado para avaliar uma condição e retornar um dos dois valores possíveis,
// dependendo do resultado da condição
// condição ? expressão1 : expressão2;

let idade = 17;
let mensagem = idade >= 18 ? "Maior de idade" : "Menor de idade";

// if (idade >= 18) {
// 	mensagem = "Maior de idade";
// } else {
// 	mensagem = "Menor de idade";
// }

console.log(mensagem);
